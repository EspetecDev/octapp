---
phase: 05-railway-deploy-smoke-test
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02

user_setup:
  - service: railway
    why: "Deploy target — hosts the Bun WebSocket server and SvelteKit static build"
    env_vars:
      - name: ADMIN_TOKEN
        value: "ebf5c8b171e554b037df2d4225b3bc65"
        source: "Pre-generated in this plan — copy the value above exactly"
    dashboard_config:
      - task: "Create Railway project and connect GitHub repo"
        location: "railway.app → New Project → Deploy from GitHub repo"
      - task: "Set ADMIN_TOKEN using Raw Editor"
        location: "Railway service → Variables tab → Raw Editor"
      - task: "Generate public domain"
        location: "Railway service → Settings → Networking → Public Networking → Generate Domain"

must_haves:
  truths:
    - "Railway project exists and is connected to the GitHub repo on branch main"
    - "ADMIN_TOKEN is set in Railway Variables with the exact value ebf5c8b171e554b037df2d4225b3bc65 (no trailing whitespace)"
    - "First deploy succeeds (Railway shows green health check)"
    - "A public Railway domain is generated and the URL is known"
    - "Railway logs show 'Admin token: ***' (masked, not '(not set)') on first deploy"
  artifacts:
    - path: ".planning/phases/05-railway-deploy-smoke-test/05-02-SUMMARY.md"
      provides: "The live Railway URL recorded for use in smoke test (Plan 03)"
  key_links:
    - from: "GitHub repo main branch"
      to: "Railway deploy pipeline"
      via: "GitHub integration auto-deploy on push"
    - from: "ADMIN_TOKEN env var"
      to: "server/index.ts admin gate"
      via: "process.env.ADMIN_TOKEN at runtime"
---

<objective>
Create the Railway project, connect it to GitHub, set ADMIN_TOKEN securely, trigger the first deploy, and generate a public domain.

Purpose: This is the operational step that puts the app on the internet. All subsequent smoke testing (Plan 03) and multi-device testing (Phase 6) depend on having a live URL.
Output: A public Railway URL (`https://<name>.up.railway.app`) with the app running and ADMIN_TOKEN confirmed in Railway logs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/05-railway-deploy-smoke-test/05-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create Railway project and connect GitHub repo</name>
  <files></files>
  <read_first>.planning/phases/05-railway-deploy-smoke-test/05-01-SUMMARY.md</read_first>
  <action>Human-action checkpoint: no automated action. Follow the what-to-do steps below.</action>
  <what-to-do>
Follow these exact steps in the Railway dashboard (https://railway.app):

**Step 1 — Create project:**
1. Log in to https://railway.app
2. Click "New Project" (top right)
3. Select "Deploy from GitHub repo"
4. Authorize Railway to access your GitHub account if not already done
5. Search for the `octapp` repo and select it
6. Railway will ask which branch — select **main**
7. Railway will begin the first deploy automatically

**Step 2 — Wait for first build:**
- The first build pulls the Docker image and runs `bun run build` — takes 2-4 minutes
- Watch the build logs for: `[octapp] Server running on port 8080`
- If the build fails, check the build logs for the error and report back

**Step 3 — Generate a public domain:**
1. Click on the deployed service in the Railway dashboard
2. Go to Settings → Networking → Public Networking
3. Click "Generate Domain"
4. Railway assigns a URL like `https://octapp-production-xxxx.up.railway.app`
5. Copy this URL — you will need it for every step in Plan 03

**DO NOT** set ADMIN_TOKEN yet — that is Task 2.
  </what-to-do>
  <verify>Human confirms Railway URL is live by pasting it as the resume signal.</verify>
  <done>Railway project exists, GitHub repo connected to main, first deploy succeeded, public domain generated.</done>
  <acceptance_criteria>
    - User has pasted a Railway URL in the form https://*.up.railway.app
    - Railway dashboard shows the service as healthy (green health check)
    - Railway build logs show "[octapp] Server running on port 8080"
  </acceptance_criteria>
  <resume-signal>Paste the generated Railway URL (e.g., https://octapp-production-xxxx.up.railway.app) to continue</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Set ADMIN_TOKEN in Railway Variables (Raw Editor — no trailing whitespace)</name>
  <files></files>
  <read_first>.planning/research/PITFALLS.md</read_first>
  <action>Human-action checkpoint: no automated action. Follow the what-to-do steps below.</action>
  <what-to-do>
**CRITICAL: Use Raw Editor only — the standard input field introduces trailing whitespace that breaks the token comparison.**

Your ADMIN_TOKEN is: `ebf5c8b171e554b037df2d4225b3bc65`

Save this token somewhere safe (password manager, notes) — you will need it to access the admin dashboard at the event.

**Steps:**
1. In the Railway dashboard, click on your service
2. Go to the **Variables** tab
3. Click **Raw Editor** (pencil icon, usually top-right of the variables panel)
4. In the raw editor text area, add exactly this line (no spaces before or after the `=`, no trailing space after the value):
   ```
   ADMIN_TOKEN=ebf5c8b171e554b037df2d4225b3bc65
   ```
5. Click **Update Variables**
6. Railway will **redeploy automatically** after saving variables

**After redeploy completes (~2 min), check Railway service logs:**
1. Click the service → Logs tab
2. Look for this exact line: `[octapp] Admin token: ***`
3. If you see `[octapp] Admin token: (not set)` — the variable was not applied. Redeploy manually (Deployments tab → Deploy button) and check logs again.

**DO NOT** click "Seal" on the variable yet — sealed variables cannot be read back to verify. Wait until after smoke testing confirms the token works.

**Your admin bookmark URL (save this now):**
`https://<your-railway-domain>/admin?token=ebf5c8b171e554b037df2d4225b3bc65`

Replace `<your-railway-domain>` with the URL from Task 1.
  </what-to-do>
  <verify>Human confirms Railway logs show "Admin token: ***" as the resume signal.</verify>
  <done>ADMIN_TOKEN is set in Railway Variables with the exact value, no trailing whitespace. Railway logs confirm it is masked (not "(not set)"). Admin bookmark URL is saved.</done>
  <acceptance_criteria>
    - Railway logs show "[octapp] Admin token: ***" (three asterisks — masked)
    - Railway logs do NOT show "[octapp] Admin token: (not set)"
    - User has saved the admin bookmark URL with the token
  </acceptance_criteria>
  <resume-signal>Type "token confirmed" after you see `Admin token: ***` in Railway logs, or describe any issue seen</resume-signal>
</task>

</tasks>

<verification>
Before completing this plan:
1. Railway URL is reachable in a browser (page loads — even if just a blank/error page is fine here, connection confirmed)
2. Railway logs show `[octapp] Admin token: ***` (not `(not set)`)
3. Railway logs show `[octapp] Server running on port 8080`
4. Railway health check is green (Service overview shows a green dot or "Healthy" status)
</verification>

<success_criteria>
1. Public Railway URL is live and accessible
2. Railway logs confirm ADMIN_TOKEN is set (masked, not "(not set)")
3. Railway health check passes (green in dashboard)

The URL and token value are recorded in the plan summary for use in Plan 03 smoke testing.
</success_criteria>

<output>
After completion, create `.planning/phases/05-railway-deploy-smoke-test/05-02-SUMMARY.md`

Include in the summary:
- The live Railway URL (e.g., https://octapp-production-xxxx.up.railway.app)
- Confirmation that logs show `Admin token: ***`
- The full admin bookmark URL with token
</output>
