---
phase: 11-i18n-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/app.html
  - src/routes/+page.svelte
autonomous: false
requirements:
  - INFRA-05
must_haves:
  truths:
    - "app.html has an inline <script> before %sveltekit.head% that sets document.documentElement.lang and window.__initialLocale synchronously"
    - "The inline script reads localStorage 'octapp:locale', validates against ['en','ca','es'], falls back to navigator.language matching, never throws"
    - "All five existing <meta> tags are preserved in app.html (charset, viewport, theme-color, apple-mobile-web-app-capable, apple-mobile-web-app-status-bar-style)"
    - "The join page shows a 3-button locale toggle strip at the bottom (outside the form), visually confirming locale reactivity"
    - "Clicking en/ca/es buttons changes the displayed test_locale_active string without a page reload"
    - "After selecting a locale and refreshing, the same locale is applied immediately (no FOUC)"
  artifacts:
    - path: "src/app.html"
      provides: "FOUC prevention inline script + dynamic html[lang]"
      contains: "window.__initialLocale"
    - path: "src/routes/+page.svelte"
      provides: "Temporary verification stub"
      contains: "TODO(Phase 13)"
  key_links:
    - from: "app.html inline script"
      to: "window.__initialLocale"
      via: "window.__initialLocale = locale"
      pattern: "window\\.__initialLocale"
    - from: "app.html inline script"
      to: "document.documentElement.lang"
      via: "document.documentElement.lang = locale"
      pattern: "document\\.documentElement\\.lang"
    - from: "+page.svelte verification stub"
      to: "locale.svelte.ts setLocale()"
      via: "onclick={() => setLocale('ca')}"
      pattern: "setLocale\\('(en|ca|es)'\\)"
    - from: "+page.svelte verification stub"
      to: "messages.js m.test_locale_active()"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.test_locale_active"
---

<objective>
Add the FOUC prevention inline script to `app.html` and add the temporary locale verification stub to the bottom of `+page.svelte`. Then verify the full locale switching pipeline end-to-end: click a locale button → string changes → refresh → same locale applies immediately.

Purpose: INFRA-05 (FOUC prevention) and the final proof that INFRA-01 through INFRA-04 are all working correctly together. The verification stub is a temporary fixture — Phase 13 replaces it with the real language picker UI.

Output: `app.html` with synchronous FOUC prevention, `+page.svelte` with 3-button locale strip, and confirmed visual proof that locale switching and persistence work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-i18n-infrastructure/11-CONTEXT.md
@.planning/phases/11-i18n-infrastructure/11-RESEARCH.md
@.planning/phases/11-i18n-infrastructure/11-01-SUMMARY.md
@.planning/phases/11-i18n-infrastructure/11-02-SUMMARY.md
</context>

<interfaces>
<!-- Current state of files being modified. Read before touching. -->

Current src/app.html (as of Plan 01 — scaffold may have patched it):
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="theme-color" content="#0f0f0f" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover" style="background-color: #0f0f0f; margin: 0;">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```
NOTE: The scaffold (Plan 01) may have patched app.html to add `lang="%lang%"`. If so, that attribute must be replaced — `%lang%` is an SSR-only placeholder and is never populated in an SSR-disabled SPA.

src/routes/+page.svelte (read the actual file — imports and structure may have changed):
- Has `<script lang="ts">` with imports from $lib/socket.ts, $app/navigation
- Has a `<main>` block with the join form
- The verification stub goes AFTER the closing `</main>` tag, BEFORE any `<style>` block

Already created in Plans 01 and 02:
- src/lib/i18n/locale.svelte.ts — exports: locale, setLocale, initLocale, SupportedLocale
- src/lib/paraglide/messages.js — exports: m.test_locale_active() (compiled from messages/en.json)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app.html with FOUC prevention inline script</name>
  <read_first>
    - src/app.html (MUST read actual file — scaffold may have modified it; need to see current state before overwriting)
  </read_first>
  <files>src/app.html</files>
  <action>
Read the current state of `src/app.html` first. The scaffold from Plan 01 may have added a `lang="%lang%"` attribute to `<html>` — this must be replaced. Rewrite `src/app.html` to match the following exactly:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <meta name="theme-color" content="#0f0f0f" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <!-- FOUC prevention: read locale synchronously before SPA boots (D-04, INFRA-05) -->
    <!-- Must be placed BEFORE %sveltekit.head% so it runs before SvelteKit's JS -->
    <!-- Uses ES5 syntax (var, indexOf) for maximum browser compatibility incl. old iOS Safari -->
    <script>
      (function() {
        var supported = ['en', 'ca', 'es'];
        var stored = localStorage.getItem('octapp:locale');
        var locale;
        if (stored && supported.indexOf(stored) !== -1) {
          locale = stored;
        } else {
          // First visit: derive nearest supported locale from browser language (D-07, INFRA-04)
          var lang = (navigator.language || 'en').toLowerCase();
          if (lang.indexOf('ca') === 0) locale = 'ca';
          else if (lang.indexOf('es') === 0) locale = 'es';
          else locale = 'en';
        }
        document.documentElement.lang = locale;
        window.__initialLocale = locale;
      })();
    </script>
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover" style="background-color: #0f0f0f; margin: 0;">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```

**Critical correctness checks:**
1. The `<html>` tag keeps `lang="en"` as a static fallback for the brief instant before the inline script runs — this is intentional and correct. The script immediately overwrites it.
2. The inline `<script>` block MUST be placed BEFORE `%sveltekit.head%` — if placed after, SvelteKit boots before `window.__initialLocale` is set.
3. The script uses ES5 `var` and `indexOf` — NOT ES6 `const`, `let`, or `includes()` — for compatibility with older iOS Safari versions.
4. The script sets BOTH `document.documentElement.lang` AND `window.__initialLocale` — both are required.
5. `window.__initialLocale` is read by `initLocale()` in `locale.svelte.ts` to avoid a redundant `localStorage.getItem` call.
6. The five existing `<meta>` tags must all be preserved. Do not remove any of them.
7. `body` must retain `data-sveltekit-preload-data="hover"` and `style="background-color: #0f0f0f; margin: 0;"`.

After writing, run `bun run build` to confirm the file is still valid.
  </action>
  <verify>
    <automated>grep "window.__initialLocale" src/app.html && echo "__initialLocale set OK"</automated>
    <automated>grep "document.documentElement.lang" src/app.html && echo "html[lang] set OK"</automated>
    <automated>grep "octapp:locale" src/app.html && echo "localStorage key OK"</automated>
    <automated>grep "%sveltekit.head%" src/app.html && echo "sveltekit.head present"</automated>
    <automated>grep "apple-mobile-web-app-capable" src/app.html && echo "meta tags preserved"</automated>
    <automated>bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/app.html contains `window.__initialLocale = locale`
    - src/app.html contains `document.documentElement.lang = locale`
    - src/app.html contains `localStorage.getItem('octapp:locale')`
    - src/app.html contains `%sveltekit.head%`
    - src/app.html contains `%sveltekit.body%`
    - src/app.html contains `apple-mobile-web-app-capable` (meta tag preserved)
    - src/app.html contains `apple-mobile-web-app-status-bar-style` (meta tag preserved)
    - src/app.html contains `theme-color` (meta tag preserved)
    - src/app.html contains `viewport-fit=cover` (meta tag preserved)
    - The inline `<script>` block appears BEFORE `%sveltekit.head%` in the file
    - src/app.html does NOT contain `lang="%lang%"` (scaffold placeholder removed)
    - src/app.html does NOT contain `%lang%` anywhere
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>app.html has synchronous FOUC prevention script that sets html[lang] and window.__initialLocale before SPA boots. All existing meta tags preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add temporary locale verification stub to +page.svelte</name>
  <read_first>
    - src/routes/+page.svelte (MUST read full file — need to find: end of main block, existing imports in script, presence of style block)
    - src/lib/i18n/locale.svelte.ts (confirm export names: locale, setLocale)
    - src/lib/paraglide/messages.js (confirm m.test_locale_active is exported)
  </read_first>
  <files>src/routes/+page.svelte</files>
  <action>
Modify `src/routes/+page.svelte` to:

1. Add these two imports at the end of the existing `<script lang="ts">` import block (after the last existing import, before any other code):
```typescript
import * as m from '$lib/paraglide/messages.js';
import { locale, setLocale } from '$lib/i18n/locale.svelte.ts';
```

2. Add the verification stub div AFTER the closing main tag and BEFORE any style tag (or at the end of the template if there is no style block). The stub must be placed outside the join form main element:

```svelte
<!-- TODO(Phase 13): replace with real language picker UI — remove this stub -->
<div style="position: fixed; bottom: 1rem; left: 50%; transform: translateX(-50%); display: flex; gap: 0.5rem; z-index: 999; background: #1a1a1a; padding: 0.5rem; border-radius: 0.5rem; align-items: center;">
  <span style="font-size: 0.75rem; color: #888;">{m.test_locale_active()}</span>
  <button onclick={() => setLocale('en')} style="opacity: {locale === 'en' ? 1 : 0.4}; color: white; background: none; border: 1px solid #444; border-radius: 4px; padding: 2px 8px; cursor: pointer;">en</button>
  <button onclick={() => setLocale('ca')} style="opacity: {locale === 'ca' ? 1 : 0.4}; color: white; background: none; border: 1px solid #444; border-radius: 4px; padding: 2px 8px; cursor: pointer;">ca</button>
  <button onclick={() => setLocale('es')} style="opacity: {locale === 'es' ? 1 : 0.4}; color: white; background: none; border: 1px solid #444; border-radius: 4px; padding: 2px 8px; cursor: pointer;">es</button>
</div>
```

**Rules:**
- Do NOT modify any existing imports, state, functions, or template markup in +page.svelte
- The stub uses inline styles (not Tailwind classes) — this is intentional so it is visually distinct and easy to find/remove in Phase 13
- The active locale button uses `opacity: {locale === 'xx' ? 1 : 0.4}` to show which locale is selected — this is a Svelte reactive expression inside the style string
- `m.test_locale_active()` is a Paraglide-compiled function that returns different strings in each locale
- After running `bun run build`, also run `bun run dev` and manually test locale switching (see Plan 03 checkpoint)
  </action>
  <verify>
    <automated>grep "TODO(Phase 13)" src/routes/+page.svelte && echo "removal marker OK"</automated>
    <automated>grep "m.test_locale_active" src/routes/+page.svelte && echo "test string OK"</automated>
    <automated>grep "setLocale" src/routes/+page.svelte && echo "setLocale calls present"</automated>
    <automated>grep "import \* as m from" src/routes/+page.svelte && echo "messages import OK"</automated>
    <automated>grep "locale.svelte.ts" src/routes/+page.svelte && echo "locale module import OK"</automated>
    <automated>bun run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/+page.svelte contains `import * as m from '$lib/paraglide/messages.js'`
    - src/routes/+page.svelte contains `import { locale, setLocale } from '$lib/i18n/locale.svelte.ts'`
    - src/routes/+page.svelte contains `m.test_locale_active()`
    - src/routes/+page.svelte contains `setLocale('en')`, `setLocale('ca')`, `setLocale('es')`
    - src/routes/+page.svelte contains `TODO(Phase 13)` comment marking the stub for removal
    - src/routes/+page.svelte contains reactive opacity expression for active locale button
    - All existing +page.svelte content is preserved (join form, auto-redirect logic, gameState imports, etc.)
    - `bun run build` exits 0
  </acceptance_criteria>
  <done>+page.svelte has 3-button locale stub outside the join form, importing from locale module and messages. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify locale switching, persistence, and FOUC prevention end-to-end</name>
  <files>none</files>
  <action>Run `bun run dev`, open http://localhost:5173, and follow the verification steps in how-to-verify below.</action>
  <verify>
    <automated>bun run build 2>&1 | tail -3</automated>
  </verify>
  <done>User confirms locale switching, localStorage persistence, and FOUC prevention all work correctly in the browser.</done>
  <what-built>
    Full i18n infrastructure:
    - Paraglide v2 installed and building (Plan 01)
    - Reactive locale.svelte.ts module with $state rune, localStorage persistence, browser auto-detection (Plan 02)
    - initLocale() wired in +layout.svelte onMount (Plan 02)
    - FOUC prevention inline script in app.html (Plan 03, Task 1)
    - Temporary 3-button locale verification stub on join page (Plan 03, Task 2)
  </what-built>
  <how-to-verify>
    Run the dev server: `bun run dev`
    Then open the app in a browser (http://localhost:5173).

    1. LOCALE SWITCHING: On the join page, you should see a small pill strip near the bottom of the screen showing "Active locale: English | [en] [ca] [es]". Click [ca] — the text must change to "Idioma actiu: Catala" WITHOUT a page reload. Click [es] — must change to "Idioma activo: Espanol". Click [en] — returns to English. Active button must be brighter (full opacity) than inactive ones.

    2. PERSISTENCE: After switching to [ca], reload the page (Cmd+R or F5). The strip must immediately show "Idioma actiu: Catala" — no flash of English first. Check localStorage in DevTools (Application - Local Storage - localhost:5173) — key `octapp:locale` should be `ca`.

    3. FOUC PREVENTION: Open DevTools - Elements. The html tag's `lang` attribute should already be `ca` before the SPA finishes loading (visible on hard reload). `window.__initialLocale` should be `ca` in the Console.

    4. BROWSER AUTO-DETECTION (optional): Clear localStorage (DevTools - Application - Clear Site Data). Reload. If your browser is set to English, `lang` should be `en`.

    5. BUILD CHECK: Run `bun run build` — must exit 0 with no warnings about missing locales or strategy errors.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 5 checks pass.
    If an issue exists, describe it (e.g., "clicking ca does not change the string", "lang attribute stays en after reload", "build error: ...") so the next executor can fix it.
  </resume-signal>
</task>

</tasks>

<verification>
After this plan completes (before checkpoint):
1. `grep "window.__initialLocale" src/app.html` — returns match
2. `grep "%lang%" src/app.html` — must return NO match (scaffold placeholder removed)
3. `grep "TODO(Phase 13)" src/routes/+page.svelte` — returns match
4. `grep "m.test_locale_active" src/routes/+page.svelte` — returns match
5. `bun run build` exits 0

After checkpoint:
- User has confirmed locale switching, persistence, and FOUC prevention all work correctly in the browser
</verification>

<success_criteria>
- app.html has FOUC inline script (ES5) before %sveltekit.head% setting html[lang] and window.__initialLocale
- All five original meta tags preserved in app.html
- +page.svelte has 3-button stub with TODO(Phase 13) marker, m.test_locale_active(), and reactive opacity
- Locale switching works without page reload (INFRA-02 confirmed)
- Locale persists across refresh (INFRA-03 confirmed)
- No flash of wrong locale on reload (INFRA-05 confirmed)
- vite build exits 0 (INFRA-01 confirmed end-to-end)
- User approves at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-i18n-infrastructure/11-03-SUMMARY.md`
</output>
