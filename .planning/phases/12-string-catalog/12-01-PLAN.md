---
phase: 12-string-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - src/routes/+page.svelte
  - src/lib/LandscapeOverlay.svelte
  - src/lib/ReconnectingOverlay.svelte
  - src/lib/components/MemoryMinigame.svelte
  - src/lib/components/TriviaMinigame.svelte
  - src/lib/components/ScavengerScreen.svelte
  - src/lib/components/RadialCountdown.svelte
  - src/lib/components/RewardScreen.svelte
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Join page renders with no hardcoded UI strings — all visible text, placeholders, and aria-labels come from message functions"
    - "All 7 shared components render with no hardcoded UI strings"
    - "messages/en.json contains all English strings for the join page and shared components"
    - "The Phase 11 stub key test_locale_active is NOT removed yet (removed in Plan 05)"
  artifacts:
    - path: "messages/en.json"
      provides: "English string catalog — join page + shared component strings added alongside existing stub"
    - path: "src/routes/+page.svelte"
      provides: "Join page with all hardcoded strings replaced by m.* function calls"
    - path: "src/lib/LandscapeOverlay.svelte"
      provides: "Shared overlay with m.* calls"
    - path: "src/lib/ReconnectingOverlay.svelte"
      provides: "Shared overlay with m.* calls"
    - path: "src/lib/components/MemoryMinigame.svelte"
      provides: "Game component with m.* calls"
    - path: "src/lib/components/TriviaMinigame.svelte"
      provides: "Game component with m.* calls"
    - path: "src/lib/components/ScavengerScreen.svelte"
      provides: "Game component with m.* calls"
    - path: "src/lib/components/RadialCountdown.svelte"
      provides: "Timer component with m.* calls"
    - path: "src/lib/components/RewardScreen.svelte"
      provides: "Reward component with m.* calls"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
    - from: "src/lib/components/*.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
---

<objective>
Extract all hardcoded UI strings from the join page (+page.svelte) and all 7 shared components into messages/en.json, replacing each string with a Paraglide message function call.

Purpose: I18N-01 compliance — no hardcoded strings remain in these files. This is Batch 1 of 3 extraction batches and can run in parallel with Plans 02 and 03.
Output: Updated messages/en.json with join + shared component keys; all 8 source files using m.*() calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-string-catalog/12-CONTEXT.md

<interfaces>
<!-- Message import pattern — already used in +page.svelte from Phase 11 stub -->
<!-- Import line to add to every modified .svelte file that doesn't already have it: -->
```typescript
import * as m from '$lib/paraglide/messages.js';
```

<!-- Key naming convention (Claude's discretion from CONTEXT.md): -->
<!-- Use flat snake_case with a view/component prefix. Examples: -->
<!--   join_title, join_subtitle, join_code_label, landscape_heading -->
<!-- Paraglide v2 generates a typed function for each key in messages/en.json. -->
<!-- Each key in en.json maps to a zero-argument function: m.join_title() -->
<!-- Keys with dynamic values (like "Card {i+1}") require Paraglide parameters: -->
<!--   { "card_aria_label": "Card {index}" }  →  m.card_aria_label({ index: i + 1 }) -->
<!-- BUT: for complexity, prefer splitting dynamic from static where the static part -->
<!-- can be extracted and the dynamic value renders from game state directly -->

<!-- locale.svelte.ts shape (read-only reference) -->
<!-- locale.current — reactive SupportedLocale ('en'|'ca'|'es') -->
<!-- setLocale(next: SupportedLocale) — persists + updates state -->
<!-- initLocale() — called in +layout.svelte onMount already -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add join page + shared component strings to messages/en.json</name>
  <read_first>
    - messages/en.json (current content — must be preserved, not overwritten)
    - src/routes/+page.svelte (all hardcoded strings to extract)
    - src/lib/LandscapeOverlay.svelte
    - src/lib/ReconnectingOverlay.svelte
    - src/lib/components/MemoryMinigame.svelte
    - src/lib/components/TriviaMinigame.svelte
    - src/lib/components/ScavengerScreen.svelte
    - src/lib/components/RadialCountdown.svelte
    - src/lib/components/RewardScreen.svelte
  </read_first>
  <files>messages/en.json</files>
  <action>
Replace the entire content of messages/en.json with the JSON below. It PRESERVES the existing test_locale_active key (do not remove it — Plan 05 handles removal) and adds all new keys.

Key naming convention: flat snake_case with view/component prefix.
Parameters use {paramName} syntax as Paraglide v2 expects (e.g. `"Card {index}{matched}"`).

```json
{
  "test_locale_active": "Active locale: English",

  "join_reconnecting": "Reconnecting...",
  "join_title": "Bachelor Party",
  "join_subtitle": "Enter your code to join the game.",
  "join_code_label": "Game code",
  "join_name_label": "Your name",
  "join_name_placeholder": "Your name",
  "join_role_label": "Your role",
  "join_groom_btn_loading": "...",
  "join_groom_btn": "I'm the Groom",
  "join_groom_title_loading": "Connecting...",
  "join_groom_title_taken": "Groom role already taken",
  "join_group_btn": "I'm in the Group",
  "join_cta_btn": "Join Game",
  "join_error_generic": "Something went wrong. Try again.",
  "join_error_timeout": "Connection timed out. Try again.",

  "landscape_heading": "Rotate your phone",
  "landscape_body": "This game is portrait only.",

  "reconnect_aria_label": "Reconnecting to game",
  "reconnect_heading": "Reconnecting...",
  "reconnect_body": "Hold tight — we'll get you back in the game.",

  "memory_grid_aria_label": "Memory card grid",
  "memory_card_aria_label": "Card {index}",
  "memory_card_matched_suffix": ", matched",
  "memory_result_win": "NAILED IT!",
  "memory_result_loss": "TIME'S UP!",
  "memory_points_win": "+50 pts",
  "memory_points_loss": "−20 pts",

  "trivia_options_aria_label": "Answer options",
  "trivia_loading": "Loading question...",
  "trivia_result_win": "CORRECT!",
  "trivia_result_loss": "WRONG!",
  "trivia_points_win": "+50 pts",
  "trivia_points_loss": "−20 pts",

  "scavenger_clue_label": "SCAVENGER CLUE",
  "scavenger_hint_btn": "Request Hint (−10 pts)",
  "scavenger_hint_label": "HINT",
  "scavenger_found_btn": "I Found It!",

  "radial_aria_label": "{remaining} seconds remaining",

  "reward_unlocked_label": "REWARD UNLOCKED",
  "reward_chapter_label": "Chapter {number}",
  "reward_past_label": "Past Rewards",
  "reward_past_chapter_btn": "Chapter {number}: {name}"
}
```

NOTE on dynamic keys:
- `memory_card_aria_label`: used as `m.memory_card_aria_label({ index: i + 1 })` — produces "Card 1", "Card 2", etc.
- `memory_card_matched_suffix`: concatenated in the aria-label when matched: `m.memory_card_aria_label({ index: i + 1 }) + (card.matched ? m.memory_card_matched_suffix() : '')`
- `radial_aria_label`: used as `m.radial_aria_label({ remaining })` — produces "{N} seconds remaining"
- `reward_chapter_label`: used as `m.reward_chapter_label({ number: activeChapterIndex + 1 })`
- `reward_past_chapter_btn`: used as `m.reward_past_chapter_btn({ number: i + 1, name: pastChapter.name })` — note: `name` is user-authored content passed as a parameter, not hardcoded in the catalog

IMPORTANT: `reward_past_chapter_btn` passes chapter.name (user content) as a parameter to the message function. The static template "Chapter {number}: {name}" is the translatable part; the actual chapter name remains from game state. This satisfies I18N-04 (user content excluded from catalog).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const j=JSON.parse(f.readFileSync('messages/en.json'));const keys=['test_locale_active','join_title','join_groom_btn','join_error_timeout','landscape_heading','reconnect_heading','memory_result_win','trivia_result_win','scavenger_found_btn','radial_aria_label','reward_unlocked_label'];const missing=keys.filter(k=>!j[k]);if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK: all required keys present, total keys:',Object.keys(j).length)"</automated>
  </verify>
  <done>messages/en.json contains all join page and shared component keys (≥30 keys total) plus preserved test_locale_active key. Node validation script exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in join page (+page.svelte)</name>
  <read_first>
    - src/routes/+page.svelte (full file — executor must read current state before modifying)
    - messages/en.json (to verify key names match exactly)
  </read_first>
  <files>src/routes/+page.svelte</files>
  <action>
The file already has `import * as m from '$lib/paraglide/messages.js'` (added in Phase 11 for the stub). Do NOT add a duplicate import.

Replace every hardcoded UI string in the file with the corresponding m.*() call. The strings and their replacements are:

TEMPLATE replacements (in the `{:else}` block and beyond):
1. `<p class="text-base text-text-secondary">Reconnecting...</p>`
   → `<p class="text-base text-text-secondary">{m.join_reconnecting()}</p>`

2. `<h1 class="text-[40px] font-bold text-text-primary leading-tight">Bachelor Party</h1>`
   → `<h1 class="text-[40px] font-bold text-text-primary leading-tight">{m.join_title()}</h1>`

3. `<p class="text-base text-text-secondary mt-1">Enter your code to join the game.</p>`
   → `<p class="text-base text-text-secondary mt-1">{m.join_subtitle()}</p>`

4. `<label for="code-input" class="text-[14px] text-text-secondary">Game code</label>`
   → `<label for="code-input" class="text-[14px] text-text-secondary">{m.join_code_label()}</label>`

5. `<label for="name-input" class="text-[14px] text-text-secondary">Your name</label>`
   → `<label for="name-input" class="text-[14px] text-text-secondary">{m.join_name_label()}</label>`

6. `placeholder="Your name"` (on the name input)
   → `placeholder={m.join_name_placeholder()}`

7. `<span class="text-[14px] text-text-secondary">Your role</span>`
   → `<span class="text-[14px] text-text-secondary">{m.join_role_label()}</span>`

8. Groom button content (currently `{gameStateLoading ? "..." : "I'm the Groom"}`):
   → `{gameStateLoading ? m.join_groom_btn_loading() : m.join_groom_btn()}`

9. Groom button title attribute (currently `title={gameStateLoading ? "Connecting..." : groomTaken ? "Groom role already taken" : undefined}`):
   → `title={gameStateLoading ? m.join_groom_title_loading() : groomTaken ? m.join_groom_title_taken() : undefined}`

10. Group button content (`I'm in the Group`):
    → `{m.join_group_btn()}`

11. Submit button text (`Join Game`):
    → `{m.join_cta_btn()}`

SCRIPT block replacements:
12. In `lastError.subscribe` callback, the fallback string:
    `submitError = err.message ?? "Something went wrong. Try again.";`
    → `submitError = err.message ?? m.join_error_generic();`

13. In `handleSubmit`, the timeout string:
    `submitError = "Connection timed out. Try again.";`
    → `submitError = m.join_error_timeout();`

IMPORTANT: The placeholder `placeholder="XXXXXX"` on the code input is NOT a translatable UI string — it is a format mask showing the 6-character pattern. Leave it as-is (hardcoded "XXXXXX").

The `m.test_locale_active()` call in the Phase 11 stub block at the bottom of the file must remain unchanged — it is removed in Phase 13 as documented by the TODO comment.

Do NOT change any Tailwind classes, event handlers, logic, or the {#if}/{:else} structure.
  </action>
  <verify>
    <automated>grep -n '"Bachelor Party"\|"Enter your code\|"Game code"\|"Your name"\|"Your role"\|"Groom role already taken"\|"Something went wrong\|"Connection timed out\|"Join Game"\|"Connecting\.\.\."' src/routes/+page.svelte</automated>
  </verify>
  <done>grep for the listed hardcoded strings returns zero matches in src/routes/+page.svelte. All replaced with m.*() calls. The XXXXXX placeholder and all non-UI strings (class names, types, socket message types) remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded strings in shared components</name>
  <read_first>
    - src/lib/LandscapeOverlay.svelte
    - src/lib/ReconnectingOverlay.svelte
    - src/lib/components/MemoryMinigame.svelte
    - src/lib/components/TriviaMinigame.svelte
    - src/lib/components/ScavengerScreen.svelte
    - src/lib/components/RadialCountdown.svelte
    - src/lib/components/RewardScreen.svelte
    - messages/en.json (to verify key names)
  </read_first>
  <files>
    src/lib/LandscapeOverlay.svelte,
    src/lib/ReconnectingOverlay.svelte,
    src/lib/components/MemoryMinigame.svelte,
    src/lib/components/TriviaMinigame.svelte,
    src/lib/components/ScavengerScreen.svelte,
    src/lib/components/RadialCountdown.svelte,
    src/lib/components/RewardScreen.svelte
  </files>
  <action>
Add `import * as m from '$lib/paraglide/messages.js';` to the `<script>` block of each file that does not already have it. LandscapeOverlay.svelte has no script block — add one: `<script lang="ts">import * as m from '$lib/paraglide/messages.js';</script>`.

Then apply the following replacements per file:

--- LandscapeOverlay.svelte ---
1. `<h2>Rotate your phone</h2>` → `<h2>{m.landscape_heading()}</h2>`
2. `<p>This game is portrait only.</p>` → `<p>{m.landscape_body()}</p>`

--- ReconnectingOverlay.svelte ---
3. `aria-label="Reconnecting to game"` → `aria-label={m.reconnect_aria_label()}`
4. `<h2 class="text-2xl font-bold text-text-primary">Reconnecting...</h2>`
   → `<h2 class="text-2xl font-bold text-text-primary">{m.reconnect_heading()}</h2>`
5. `Hold tight — we'll get you back in the game.`
   → `{m.reconnect_body()}`

--- MemoryMinigame.svelte ---
6. `aria-label="Memory card grid"` → `aria-label={m.memory_grid_aria_label()}`
7. `aria-label="Card {i + 1}{card.matched ? ', matched' : ''}"` (on card-outer div)
   → `aria-label={m.memory_card_aria_label({ index: i + 1 }) + (card.matched ? m.memory_card_matched_suffix() : '')}`
8. `{resultState === "win" ? "NAILED IT!" : "TIME'S UP!"}` (result-heading p tag)
   → `{resultState === "win" ? m.memory_result_win() : m.memory_result_loss()}`
9. `{resultState === "win" ? "+50 pts" : "−20 pts"}` (result-points p tag)
   → `{resultState === "win" ? m.memory_points_win() : m.memory_points_loss()}`

--- TriviaMinigame.svelte ---
10. `aria-label="Answer options"` → `aria-label={m.trivia_options_aria_label()}`
11. `<p class="text-base text-text-secondary text-center">Loading question...</p>`
    → `<p class="text-base text-text-secondary text-center">{m.trivia_loading()}</p>`
12. `{resultState === "win" ? "CORRECT!" : "WRONG!"}` (result-heading p tag)
    → `{resultState === "win" ? m.trivia_result_win() : m.trivia_result_loss()}`
13. `{resultState === "win" ? "+50 pts" : "−20 pts"}` (result-points p tag)
    → `{resultState === "win" ? m.trivia_points_win() : m.trivia_points_loss()}`

--- ScavengerScreen.svelte ---
14. `<p class="section-label">SCAVENGER CLUE</p>` → `<p class="section-label">{m.scavenger_clue_label()}</p>`
15. `Request Hint (−10 pts)` (hint-btn text) → `{m.scavenger_hint_btn()}`
16. `<p class="hint-label">HINT</p>` → `<p class="hint-label">{m.scavenger_hint_label()}</p>`
17. `I Found It!` (found-btn text) → `{m.scavenger_found_btn()}`
NOTE: `chapter.scavengerClue` and `chapter.scavengerHint` are user-authored content — leave them as-is (D-03, I18N-04).

--- RadialCountdown.svelte ---
18. `aria-label="{remaining} seconds remaining"` → `aria-label={m.radial_aria_label({ remaining })}`
NOTE: The numeric `{remaining}` text node inside the SVG `<text>` element is NOT a translatable UI string — it displays the countdown number from game state. Leave it as-is.

--- RewardScreen.svelte ---
19. `<p class="reward-label">REWARD UNLOCKED</p>` → `<p class="reward-label">{m.reward_unlocked_label()}</p>`
20. `<p class="chapter-name">Chapter {activeChapterIndex + 1}</p>`
    → `<p class="chapter-name">{m.reward_chapter_label({ number: activeChapterIndex + 1 })}</p>`
21. `<p class="section-label">Past Rewards</p>` → `<p class="section-label">{m.reward_past_label()}</p>`
22. In the accordion-trigger button (currently `Chapter {i + 1}: {pastChapter.name}`):
    → `{m.reward_past_chapter_btn({ number: i + 1, name: pastChapter.name })}`
    The chapter.name value is user-authored content passed as a parameter (I18N-04 compliant).
23. `<p class="accordion-text">{pastChapter.reward}</p>` — this is user-authored reward text. Leave as-is (I18N-04).

Do NOT change: CSS classes, Svelte logic blocks, event handlers, `chapter.reward`, `chapter.scavengerClue`, `chapter.scavengerHint`, `chapter.name`, or any emoji characters used as game content.
  </action>
  <verify>
    <automated>grep -rn '"Rotate your phone"\|"This game is portrait\|"Reconnecting to game"\|"Hold tight\|"Memory card grid\|"NAILED IT\|"TIME.S UP\|"CORRECT!\|"WRONG!\|"Loading question\|"SCAVENGER CLUE\|"Request Hint\|"I Found It\|"seconds remaining\|"REWARD UNLOCKED\|"Past Rewards"' src/lib/LandscapeOverlay.svelte src/lib/ReconnectingOverlay.svelte src/lib/components/</automated>
  </verify>
  <done>grep returns zero matches for the listed hardcoded strings across all 7 component files. All replaced with m.*() calls. User-authored content (chapter.reward, chapter.scavengerClue, chapter.name) untouched.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the build to confirm no TypeScript/Svelte compilation errors from the message function calls:

```bash
cd /Users/espetec/dev/octapp && npm run build 2>&1 | tail -20
```

A clean build (exit 0) confirms Paraglide generated the message functions for every key used in the templates.

Also run the full grep audit for remaining hardcoded strings in these files:
```bash
grep -rn '"[A-Z][a-z]\|"[A-Z][A-Z]' src/routes/+page.svelte src/lib/LandscapeOverlay.svelte src/lib/ReconnectingOverlay.svelte src/lib/components/ --include="*.svelte" | grep -v "//\|style=\|class=\|'groom'\|'group'\|'trivia'\|'memory'\|aria-pressed\|disabled\|tabindex\|role="
```
</verification>

<success_criteria>
1. messages/en.json contains ≥30 keys including test_locale_active, all join_* keys, landscape_* keys, reconnect_* keys, memory_* keys, trivia_* keys, scavenger_* keys, radial_* keys, and reward_* keys.
2. grep for hardcoded UI strings in the 8 modified .svelte files returns zero matches for the strings listed in the verify blocks above.
3. `npm run build` exits 0 — Paraglide generates typed functions for every key, TypeScript sees no unknown function calls.
4. User-authored content (chapter.reward, chapter.scavengerClue, chapter.scavengerHint, chapter.name) is not present in messages/en.json and remains as-is in templates.
</success_criteria>

<output>
After completion, create `.planning/phases/12-string-catalog/12-01-SUMMARY.md`
</output>
