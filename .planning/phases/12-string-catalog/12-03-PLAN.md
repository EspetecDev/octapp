---
phase: 12-string-catalog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - src/routes/admin/+page.svelte
  - src/routes/admin/setup/+page.svelte
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Admin dashboard renders with no hardcoded UI strings — all visible text, aria-labels, and confirm dialogs use message functions"
    - "Admin setup renders with no hardcoded UI strings — all labels, placeholders, section headers, and button text use message functions"
    - "messages/en.json contains all English strings for admin dashboard and admin setup"
    - "User-authored content (chapter names, power-up names, trivia question text) remains as form input values from state, not in the catalog"
  artifacts:
    - path: "messages/en.json"
      provides: "English string catalog — admin_* keys added (disjoint from Plans 01 and 02 keys)"
    - path: "src/routes/admin/+page.svelte"
      provides: "Admin dashboard with all hardcoded strings replaced by m.* calls"
    - path: "src/routes/admin/setup/+page.svelte"
      provides: "Admin setup with all hardcoded strings replaced by m.* calls"
  key_links:
    - from: "src/routes/admin/+page.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
    - from: "src/routes/admin/setup/+page.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
---

<objective>
Extract all hardcoded UI strings from the admin dashboard (src/routes/admin/+page.svelte) and admin setup (src/routes/admin/setup/+page.svelte) into messages/en.json, replacing each string with a Paraglide message function call.

Purpose: I18N-01 compliance for admin views. This is Batch 3 of 3 extraction batches and runs in parallel with Plans 01 and 02.
Output: Updated messages/en.json with admin_* keys; both admin route files using m.*() calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-string-catalog/12-CONTEXT.md

<interfaces>
<!-- Message import pattern -->
```typescript
import * as m from '$lib/paraglide/messages.js';
```

<!-- Key naming convention: flat snake_case with admin_ prefix -->
<!-- admin_dash_* for admin dashboard, admin_setup_* for admin setup -->

<!-- CRITICAL — user-authored content NOT to extract (I18N-04, D-03): -->
<!-- - chapter.name, chapter.scavengerClue, chapter.scavengerHint, chapter.reward -->
<!--   These are the VALUE of admin form inputs — they come from game state / user typing -->
<!--   The placeholder text and label text ARE extractable (they are static UI copy) -->
<!-- - powerUp.name, powerUp.description — user-authored game content -->
<!-- - question.question, question.correctAnswer, question.wrongOptions — trivia authored by user -->
<!-- - player.name — player names from game state -->
<!-- - sessionCode — dynamic value, not UI copy -->

<!-- NOTE: admin/+page.svelte uses confirm() dialog — extract its string too -->
<!-- The browser confirm() call: confirm("Reset the game? All players will be disconnected...") -->
<!-- This is a programmatic string in the script block — Layer 3 extraction per D-02 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin dashboard + admin setup strings to messages/en.json</name>
  <read_first>
    - messages/en.json (current content — must be preserved and extended)
    - src/routes/admin/+page.svelte (identify all hardcoded UI strings)
    - src/routes/admin/setup/+page.svelte (identify all hardcoded UI strings)
  </read_first>
  <files>messages/en.json</files>
  <action>
Read the current content of messages/en.json first. MERGE all new keys below into the existing JSON object (do not delete any existing keys).

ADMIN DASHBOARD KEYS:
```json
"admin_dash_share_code_label": "Share this code",
"admin_dash_players_join_url": "Players join at this URL",
"admin_dash_groom_role_claimed": "Groom role claimed",
"admin_dash_configure_link": "Configure Game",
"admin_dash_game_progress_header": "Game Progress",
"admin_dash_no_chapters": "No chapters configured. Tap Configure Game to set up before the event.",
"admin_dash_chapters_ready": "Chapters ready: {count}",
"admin_dash_chapter_active": "Chapter {current} of {total} — active",
"admin_dash_unlock_first": "Unlock Chapter 1",
"admin_dash_unlock_next": "Unlock Chapter {number}",
"admin_dash_all_complete": "All chapters complete.",
"admin_dash_confirm_found_btn": "Confirm Found (admin override)",
"admin_dash_repeat_chapter_btn": "Repeat Chapter {number}",
"admin_dash_reset_btn": "Reset Game",
"admin_dash_reset_confirm": "Reset the game? All players will be disconnected and scores cleared. Chapter setup is kept.",
"admin_dash_players_header": "Players in lobby",
"admin_dash_waiting_heading": "Waiting for players",
"admin_dash_waiting_body": "Share the code above. Players join at this URL.",
"admin_dash_no_players": "No players connected yet.",
"admin_dash_scores_header": "Scores",
"admin_dash_access_denied": "Access denied.",
"admin_dash_role_groom": "Groom",
"admin_dash_role_group": "Group",
"admin_dash_session_aria_label": "Session code: {code}"
```

ADMIN SETUP KEYS:
```json
"admin_setup_page_title": "Configure Game",
"admin_setup_back_link": "Back to Dashboard",
"admin_setup_no_chapters_heading": "No chapters yet",
"admin_setup_no_chapters_body": "Add your first chapter to get started.",
"admin_setup_chapter_label": "Chapter {number}",
"admin_setup_remove_chapter_btn": "Remove Chapter",
"admin_setup_chapter_name_placeholder": "Chapter name (e.g. The Bar)",
"admin_setup_minigame_trivia": "Trivia",
"admin_setup_minigame_memory": "Memory",
"admin_setup_trivia_section_label": "Trivia Questions",
"admin_setup_question_label": "Question {number}",
"admin_setup_remove_question_btn": "Remove",
"admin_setup_question_placeholder": "Question text",
"admin_setup_correct_answer_placeholder": "Correct answer",
"admin_setup_wrong_option_1_placeholder": "Wrong option 1",
"admin_setup_wrong_option_2_placeholder": "Wrong option 2",
"admin_setup_wrong_option_3_placeholder": "Wrong option 3",
"admin_setup_add_question_btn": "+ Add Question",
"admin_setup_scavenger_clue_label": "Scavenger Clue",
"admin_setup_scavenger_clue_placeholder": "Riddle or clue directing the groom where to go",
"admin_setup_hint_label": "Hint (optional)",
"admin_setup_hint_placeholder": "Optional hint (costs points to reveal)",
"admin_setup_reward_label": "Reward",
"admin_setup_reward_placeholder": "What is unlocked (dare, location, embarrassing content)",
"admin_setup_add_chapter_btn": "+ Add Chapter",
"admin_setup_powerups_section_title": "Power-ups & Sabotages",
"admin_setup_starting_tokens_label": "Starting tokens per chapter",
"admin_setup_starting_tokens_placeholder": "e.g. 10",
"admin_setup_entry_label": "Entry {number}",
"admin_setup_remove_powerup_btn": "Remove",
"admin_setup_powerup_name_placeholder": "Power-up name",
"admin_setup_powerup_desc_placeholder": "Description",
"admin_setup_cost_label": "Cost",
"admin_setup_effect_label": "Effect",
"admin_setup_effect_timer_add": "Timer Add",
"admin_setup_effect_scramble": "Scramble Options",
"admin_setup_effect_distraction": "Distraction",
"admin_setup_add_powerup_btn": "+ Add Power-up",
"admin_setup_import_btn": "Import Config",
"admin_setup_imported_btn": "Imported!",
"admin_setup_export_btn": "Export Config",
"admin_setup_exported_btn": "Exported!",
"admin_setup_save_btn": "Save Setup",
"admin_setup_saved_btn": "Saved",
"admin_setup_replace_confirm": "Replace setup?",
"admin_setup_cancel_btn": "Cancel",
"admin_setup_yes_replace_btn": "Yes, Replace",
"admin_setup_access_denied": "Access denied.",
"admin_setup_import_error_invalid_json": "File is not valid JSON"
```

NOTE: admin_dash_access_denied and admin_setup_access_denied have the same English value but are separate keys for independent translation.

NOTE: The confirm() dialog string (`admin_dash_reset_confirm`) is a programmatic Layer 3 string — it lives in the script block's `resetGame()` function and must be extracted (per D-02).

NOTE: The `importError = "File is not valid JSON"` string in admin/setup/+page.svelte is another programmatic Layer 3 string — extracted as admin_setup_import_error_invalid_json.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const j=JSON.parse(f.readFileSync('messages/en.json'));const keys=['admin_dash_share_code_label','admin_dash_reset_confirm','admin_dash_access_denied','admin_setup_page_title','admin_setup_chapter_name_placeholder','admin_setup_effect_timer_add','admin_setup_import_btn','admin_setup_save_btn','admin_setup_import_error_invalid_json'];const missing=keys.filter(k=>!j[k]);if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK — admin keys present, total keys:',Object.keys(j).length)"</automated>
  </verify>
  <done>messages/en.json contains all admin_dash_* and admin_setup_* keys listed above. Node validation exits 0. Total key count is higher than after Plans 01+02 (no keys were deleted).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in admin dashboard</name>
  <read_first>
    - src/routes/admin/+page.svelte (full file — read before modifying)
    - messages/en.json (verify admin_dash_* key names)
  </read_first>
  <files>src/routes/admin/+page.svelte</files>
  <action>
Add `import * as m from '$lib/paraglide/messages.js';` to the `<script lang="ts">` block imports. Check first — do not add duplicates.

SCRIPT BLOCK replacement (Layer 3 programmatic string):
1. In `resetGame()` function, the confirm() string:
   `if (!confirm("Reset the game? All players will be disconnected and scores cleared. Chapter setup is kept.")) return;`
   → `if (!confirm(m.admin_dash_reset_confirm())) return;`

TEMPLATE replacements:

Loading/unauthorized states:
(No string to replace — the loading spinner has no text; the unauthorized state has "Access denied." below)
2. Unauthorized p tag:
   `<p class="text-[24px] font-bold text-text-primary">Access denied.</p>`
   → `<p class="text-[24px] font-bold text-text-primary">{m.admin_dash_access_denied()}</p>`

Session code section:
3. Share this code label:
   `<p class="text-[14px] text-text-secondary mb-1">Share this code</p>`
   → `<p class="text-[14px] text-text-secondary mb-1">{m.admin_dash_share_code_label()}</p>`

4. Session code aria-label:
   `aria-label="Session code: {sessionCode}"`
   → `aria-label={m.admin_dash_session_aria_label({ code: sessionCode ?? '' })}`

5. Players join at this URL text:
   `<p class="text-base text-text-secondary mt-1">Players join at this URL</p>`
   → `<p class="text-base text-text-secondary mt-1">{m.admin_dash_players_join_url()}</p>`

6. Groom role claimed text:
   `<p class="text-[14px] text-text-secondary">Groom role claimed</p>`
   → `<p class="text-[14px] text-text-secondary">{m.admin_dash_groom_role_claimed()}</p>`

Configure Game link:
7. `Configure Game` (anchor text inside section)
   → `{m.admin_dash_configure_link()}`

Game Progress section:
8. Game Progress header:
   `<p class="text-[14px] text-text-secondary mb-3">Game Progress</p>`
   → `<p class="text-[14px] text-text-secondary mb-3">{m.admin_dash_game_progress_header()}</p>`

9. No chapters configured text:
   `<p class="text-base text-text-secondary">No chapters configured. Tap Configure Game to set up before the event.</p>`
   → `<p class="text-base text-text-secondary">{m.admin_dash_no_chapters()}</p>`

10. Chapters ready text:
    `<p class="text-base text-text-primary mb-3">Chapters ready: {chapterCount}</p>`
    → `<p class="text-base text-text-primary mb-3">{m.admin_dash_chapters_ready({ count: chapterCount })}</p>`

11. Active chapter text:
    `Chapter {activeChapterIndex + 1} of {chapterCount} — active`
    → `{m.admin_dash_chapter_active({ current: (activeChapterIndex ?? 0) + 1, total: chapterCount })}`

12. Unlock button (two variants in the same button via ternary):
    `{activeChapterIndex === null ? "Unlock Chapter 1" : \`Unlock Chapter ${(activeChapterIndex ?? -1) + 2}\`}`
    → `{activeChapterIndex === null ? m.admin_dash_unlock_first() : m.admin_dash_unlock_next({ number: (activeChapterIndex ?? -1) + 2 })}`

13. All chapters complete text:
    `<p class="text-[14px] text-text-secondary">All chapters complete.</p>`
    → `<p class="text-[14px] text-text-secondary">{m.admin_dash_all_complete()}</p>`

14. Confirm Found button:
    `Confirm Found (admin override)`
    → `{m.admin_dash_confirm_found_btn()}`

15. Repeat Chapter button:
    `Repeat Chapter {activeChapterIndex + 1}`
    → `{m.admin_dash_repeat_chapter_btn({ number: (activeChapterIndex ?? 0) + 1 })}`

16. Reset Game button:
    `Reset Game`
    → `{m.admin_dash_reset_btn()}`

Players section:
17. Players in lobby header:
    `<p class="text-[14px] text-text-secondary mb-4">Players in lobby</p>`
    → `<p class="text-[14px] text-text-secondary mb-4">{m.admin_dash_players_header()}</p>`

18. Waiting for players heading:
    `<p class="text-[24px] font-bold text-text-primary mb-2">Waiting for players</p>`
    → `<p class="text-[24px] font-bold text-text-primary mb-2">{m.admin_dash_waiting_heading()}</p>`

19. Waiting body text:
    `<p class="text-base text-text-secondary">Share the code above. Players join at this URL.</p>`
    → `<p class="text-base text-text-secondary">{m.admin_dash_waiting_body()}</p>`

20. Role badge text (in player chip):
    `{player.role === "groom" ? "Groom" : "Group"}`
    → `{player.role === "groom" ? m.admin_dash_role_groom() : m.admin_dash_role_group()}`

Scores section:
21. Scores header:
    `<p class="text-[14px] text-text-secondary mb-3">Scores</p>`
    → `<p class="text-[14px] text-text-secondary mb-3">{m.admin_dash_scores_header()}</p>`

22. No players yet text:
    `<p class="text-base text-text-secondary">No players connected yet.</p>`
    → `<p class="text-base text-text-secondary">{m.admin_dash_no_players()}</p>`

LEAVE AS-IS (game state / dynamic values — not UI copy):
- `{player.name}` — player names from game state
- `{sessionCode}` — dynamic session code value
- `{scores[player.id] ?? 0}` — score values
  </action>
  <verify>
    <automated>grep -n '"Share this code\|"Players join at\|"Groom role claimed\|"Configure Game\|"Game Progress\|"No chapters configured\|"Chapters ready:\|"— active\|"Unlock Chapter\|"All chapters complete\|"Confirm Found\|"Repeat Chapter\|"Reset Game\|"Reset the game?\|"Players in lobby\|"Waiting for players\|"Share the code above\|"No players connected\|"Scores"\|"Access denied\|"Groom"\|"Group"' src/routes/admin/+page.svelte</automated>
  </verify>
  <done>grep returns zero matches for the listed hardcoded strings in admin/+page.svelte. All static UI strings and the confirm() dialog string replaced with m.*() calls. Player names, sessionCode, and score values remain as game state expressions.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded strings in admin setup</name>
  <read_first>
    - src/routes/admin/setup/+page.svelte (full file — read before modifying)
    - messages/en.json (verify admin_setup_* key names)
  </read_first>
  <files>src/routes/admin/setup/+page.svelte</files>
  <action>
Add `import * as m from '$lib/paraglide/messages.js';` to the `<script lang="ts">` block imports. Check first — do not add duplicates.

SCRIPT BLOCK replacement (Layer 3 programmatic string):
1. In `importSetup()` function:
   `importError = "File is not valid JSON";`
   → `importError = m.admin_setup_import_error_invalid_json();`

TEMPLATE replacements:

Loading/unauthorized states:
2. Unauthorized p tag:
   `<p class="text-[24px] font-bold text-text-primary">Access denied.</p>`
   → `<p class="text-[24px] font-bold text-text-primary">{m.admin_setup_access_denied()}</p>`

Page header:
3. H1 page title:
   `<h1 class="text-[24px] font-bold text-text-primary">Configure Game</h1>`
   → `<h1 class="text-[24px] font-bold text-text-primary">{m.admin_setup_page_title()}</h1>`

4. Back link text:
   `Back to Dashboard`
   → `{m.admin_setup_back_link()}`

Empty state:
5. No chapters heading:
   `<p class="text-[24px] font-bold text-text-primary mb-2">No chapters yet</p>`
   → `<p class="text-[24px] font-bold text-text-primary mb-2">{m.admin_setup_no_chapters_heading()}</p>`

6. No chapters body:
   `<p class="text-base text-text-secondary">Add your first chapter to get started.</p>`
   → `<p class="text-base text-text-secondary">{m.admin_setup_no_chapters_body()}</p>`

Chapter blocks:
7. Chapter label in header:
   `<span class="text-[14px] text-text-secondary">Chapter {i + 1}</span>`
   → `<span class="text-[14px] text-text-secondary">{m.admin_setup_chapter_label({ number: i + 1 })}</span>`

8. Remove Chapter button:
   `Remove Chapter`
   → `{m.admin_setup_remove_chapter_btn()}`

9. Chapter name input placeholder:
   `placeholder="Chapter name (e.g. The Bar)"`
   → `placeholder={m.admin_setup_chapter_name_placeholder()}`

Minigame type selector:
10. Trivia/Memory button labels (in `{#each (["trivia", "memory"] as const) as type}`):
    `{type === "trivia" ? "Trivia" : "Memory"}`
    → `{type === "trivia" ? m.admin_setup_minigame_trivia() : m.admin_setup_minigame_memory()}`

Trivia pool:
11. Trivia Questions section label:
    `<p class="text-[14px] text-text-secondary mb-2">Trivia Questions</p>`
    → `<p class="text-[14px] text-text-secondary mb-2">{m.admin_setup_trivia_section_label()}</p>`

12. Question label in each question block:
    `<span class="text-[14px] text-text-secondary">Question {qi + 1}</span>`
    → `<span class="text-[14px] text-text-secondary">{m.admin_setup_question_label({ number: qi + 1 })}</span>`

13. Remove (question) button:
    `Remove` (on the removeQuestion button)
    → `{m.admin_setup_remove_question_btn()}`

14. Question text textarea placeholder:
    `placeholder="Question text"`
    → `placeholder={m.admin_setup_question_placeholder()}`

15. Correct answer input placeholder:
    `placeholder="Correct answer"`
    → `placeholder={m.admin_setup_correct_answer_placeholder()}`

16–18. Wrong option placeholders:
    `placeholder="Wrong option 1"` → `placeholder={m.admin_setup_wrong_option_1_placeholder()}`
    `placeholder="Wrong option 2"` → `placeholder={m.admin_setup_wrong_option_2_placeholder()}`
    `placeholder="Wrong option 3"` → `placeholder={m.admin_setup_wrong_option_3_placeholder()}`

19. Add Question button:
    `+ Add Question`
    → `{m.admin_setup_add_question_btn()}`

Scavenger clue:
20. Scavenger Clue label:
    `<label class="block text-[14px] text-text-secondary mb-1">Scavenger Clue</label>`
    → `<label class="block text-[14px] text-text-secondary mb-1">{m.admin_setup_scavenger_clue_label()}</label>`

21. Scavenger clue textarea placeholder:
    `placeholder="Riddle or clue directing the groom where to go"`
    → `placeholder={m.admin_setup_scavenger_clue_placeholder()}`

Hint (optional):
22. Hint label:
    `<label class="block text-[14px] text-text-secondary mb-1">Hint (optional)</label>`
    → `<label class="block text-[14px] text-text-secondary mb-1">{m.admin_setup_hint_label()}</label>`

23. Hint input placeholder:
    `placeholder="Optional hint (costs points to reveal)"`
    → `placeholder={m.admin_setup_hint_placeholder()}`

Reward:
24. Reward label:
    `<label class="block text-[14px] text-text-secondary mb-1">Reward</label>`
    → `<label class="block text-[14px] text-text-secondary mb-1">{m.admin_setup_reward_label()}</label>`

25. Reward textarea placeholder:
    `placeholder="What is unlocked (dare, location, embarrassing content)"`
    → `placeholder={m.admin_setup_reward_placeholder()}`

Add chapter button:
26. Add Chapter button:
    `+ Add Chapter`
    → `{m.admin_setup_add_chapter_btn()}`

Power-ups section:
27. Power-ups section h2:
    `<h2 class="text-[24px] font-bold text-text-primary mb-4">Power-ups & Sabotages</h2>`
    → `<h2 class="text-[24px] font-bold text-text-primary mb-4">{m.admin_setup_powerups_section_title()}</h2>`

28. Starting tokens label:
    `<label for="starting-tokens" class="text-[14px] text-text-secondary">Starting tokens per chapter</label>`
    → `<label for="starting-tokens" class="text-[14px] text-text-secondary">{m.admin_setup_starting_tokens_label()}</label>`

29. Starting tokens placeholder:
    `placeholder="e.g. 10"`
    → `placeholder={m.admin_setup_starting_tokens_placeholder()}`

30. Entry label:
    `<span class="text-[14px] text-text-secondary">Entry {i + 1}</span>`
    → `<span class="text-[14px] text-text-secondary">{m.admin_setup_entry_label({ number: i + 1 })}</span>`

31. Remove (power-up) button:
    `Remove` (on the removePowerUp button)
    → `{m.admin_setup_remove_powerup_btn()}`

32. Power-up name placeholder:
    `placeholder="Power-up name"`
    → `placeholder={m.admin_setup_powerup_name_placeholder()}`

33. Description placeholder:
    `placeholder="Description"`
    → `placeholder={m.admin_setup_powerup_desc_placeholder()}`

34. Cost label:
    `<label class="text-[14px] text-text-secondary">Cost</label>`
    → `<label class="text-[14px] text-text-secondary">{m.admin_setup_cost_label()}</label>`

35. Effect label:
    `<label class="text-[14px] text-text-secondary">Effect</label>`
    → `<label class="text-[14px] text-text-secondary">{m.admin_setup_effect_label()}</label>`

36. Effect select options:
    `<option value="timer_add">Timer Add</option>` → `<option value="timer_add">{m.admin_setup_effect_timer_add()}</option>`
    `<option value="scramble_options">Scramble Options</option>` → `<option value="scramble_options">{m.admin_setup_effect_scramble()}</option>`
    `<option value="distraction">Distraction</option>` → `<option value="distraction">{m.admin_setup_effect_distraction()}</option>`

37. Add Power-up button:
    `+ Add Power-up`
    → `{m.admin_setup_add_powerup_btn()}`

Sticky action bar — normal mode:
38. Import Config / Imported! button:
    `{importFlash ? "Imported!" : "Import Config"}`
    → `{importFlash ? m.admin_setup_imported_btn() : m.admin_setup_import_btn()}`

39. Export Config / Exported! button:
    `{exportFlash ? "Exported!" : "Export Config"}`
    → `{exportFlash ? m.admin_setup_exported_btn() : m.admin_setup_export_btn()}`

40. Save Setup / Saved button:
    `{saveFlash ? "Saved" : "Save Setup"}`
    → `{saveFlash ? m.admin_setup_saved_btn() : m.admin_setup_save_btn()}`

Sticky action bar — confirm mode:
41. Replace setup? text:
    `<p class="flex items-center text-[14px] text-text-secondary mr-2 shrink-0">Replace setup?</p>`
    → `<p class="flex items-center text-[14px] text-text-secondary mr-2 shrink-0">{m.admin_setup_replace_confirm()}</p>`

42. Cancel button:
    `Cancel`
    → `{m.admin_setup_cancel_btn()}`

43. Yes, Replace button:
    `Yes, Replace`
    → `{m.admin_setup_yes_replace_btn()}`

LEAVE AS-IS (user-authored content from game state / form inputs — not UI copy):
- `chapter.name` (value of chapter name input — user-authored)
- `chapter.scavengerClue`, `chapter.scavengerHint`, `chapter.reward` (textarea values — user-authored)
- `question.question`, `question.correctAnswer`, `question.wrongOptions[n]` (trivia values — user-authored)
- `powerUp.name`, `powerUp.description` (power-up values — user-authored)
- `powerUp.effectType` values used in comparisons (`timer_add`, `scramble_options`, etc.) — these are enum identifiers, not UI copy
- The `importError` display text `{importError}` in the error strip — this is already an extracted variable (the assignment in the script block was extracted above)
  </action>
  <verify>
    <automated>grep -n '"Configure Game"\|"Back to Dashboard"\|"No chapters yet\|"Add your first\|"Remove Chapter\|"Chapter name\|"Trivia Questions\|"Question text\|"Correct answer\|"Wrong option\|"Add Question\|"Scavenger Clue\|"Riddle or clue\|"Hint (optional)\|"Optional hint\|"What is unlocked\|"Add Chapter\|"Power-ups &\|"Starting tokens\|"Power-up name\|"Timer Add\|"Scramble Options\|"Distraction\|"Add Power-up\|"Import Config\|"Imported!\|"Export Config\|"Exported!\|"Save Setup\|"Saved"\|"Replace setup\|"Cancel"\|"Yes, Replace\|"Access denied\|"File is not valid"' src/routes/admin/setup/+page.svelte</automated>
  </verify>
  <done>grep returns zero matches for the listed hardcoded strings in admin/setup/+page.svelte. All static UI labels, placeholders, button text, section headers, and the programmatic JSON parse error string are replaced with m.*() calls. Form values (chapter.name, powerUp.name, etc.) remain unchanged.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the build:

```bash
cd /Users/espetec/dev/octapp && npm run build 2>&1 | tail -20
```

A clean build (exit 0) confirms Paraglide generated typed message functions for all admin_* keys.

Run a full-catalog key count check:
```bash
node -e "const j=JSON.parse(require('fs').readFileSync('messages/en.json'));console.log('Total keys in en.json:',Object.keys(j).length,'\\nKeys by prefix:'); const prefixes={};Object.keys(j).forEach(k=>{const p=k.split('_')[0]+'_'+k.split('_')[1];prefixes[p]=(prefixes[p]||0)+1});console.log(JSON.stringify(prefixes,null,2))"
```
This reveals which prefixes have keys. Confirm admin_dash and admin_setup prefixes appear.
</verification>

<success_criteria>
1. messages/en.json contains all admin_dash_* and admin_setup_* keys listed in Task 1 (≥45 new keys added to existing catalog).
2. grep for hardcoded UI strings in admin/+page.svelte and admin/setup/+page.svelte returns zero matches for the strings listed in the verify blocks.
3. `npm run build` exits 0 — Paraglide compiles all admin keys, TypeScript sees no unknown function calls.
4. The programmatic confirm() string and "File is not valid JSON" script-block string are extracted (Layer 3 extraction per D-02).
5. User-authored form input values (chapter content, power-up content, trivia content) are not in messages/en.json.
</success_criteria>

<output>
After completion, create `.planning/phases/12-string-catalog/12-03-SUMMARY.md`
</output>
