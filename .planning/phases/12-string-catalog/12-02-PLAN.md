---
phase: 12-string-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - messages/en.json
  - src/routes/groom/+page.svelte
  - src/routes/party/+page.svelte
autonomous: true
requirements: [I18N-01]

must_haves:
  truths:
    - "Groom view renders with no hardcoded UI strings — all visible text and aria-labels come from message functions"
    - "Party/group view renders with no hardcoded UI strings — all visible text, aria-labels, and toast messages come from message functions"
    - "messages/en.json contains all English strings for the groom and party views (merged with keys from Plan 01 — no key collisions)"
    - "User-authored content (chapter names, power-up names, player names, reward text) renders directly from game state, not from the catalog"
  artifacts:
    - path: "messages/en.json"
      provides: "English string catalog — groom + party keys added (these keys are disjoint from Plan 01 keys)"
    - path: "src/routes/groom/+page.svelte"
      provides: "Groom view with all hardcoded strings replaced by m.* calls"
    - path: "src/routes/party/+page.svelte"
      provides: "Party view with all hardcoded strings replaced by m.* calls"
  key_links:
    - from: "src/routes/groom/+page.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
    - from: "src/routes/party/+page.svelte"
      to: "messages/en.json"
      via: "import * as m from '$lib/paraglide/messages.js'"
      pattern: "m\\.[a-z_]+()"
---

<objective>
Extract all hardcoded UI strings from the groom view (src/routes/groom/+page.svelte) and the party/group view (src/routes/party/+page.svelte) into messages/en.json, replacing each string with a Paraglide message function call.

Purpose: I18N-01 compliance for the in-game player views. This is Batch 2 of 3 extraction batches and runs in parallel with Plans 01 and 03.
Output: Updated messages/en.json with groom + party keys; both route files using m.*() calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-string-catalog/12-CONTEXT.md

<interfaces>
<!-- Message import pattern (same as Plan 01) -->
```typescript
import * as m from '$lib/paraglide/messages.js';
```

<!-- Key naming convention: flat snake_case with view prefix -->
<!-- groom_* for groom view strings, party_* for party view strings -->
<!-- Dynamic keys use {paramName} Paraglide parameter syntax -->

<!-- CRITICAL — user-authored content NOT to extract (I18N-04, D-03): -->
<!-- - player.name, myPlayer.name, activatingPlayerName — player names from game state -->
<!-- - activeChapter.name, $gameState.chapters[n].name — chapter names from game state -->
<!-- - announcementData.powerUpName, powerUp.name — power-up names from game state -->
<!-- - powerUp.description — power-up descriptions from game state -->
<!-- - action.playerName, action.powerUpName — from game state recentActions -->
<!-- These all render as-is from game state. They are NOT in the catalog. -->

<!-- ALSO NOT EXTRACTED: -->
<!-- - chapterLabel derived value (already builds from game state + chapter name) -->
<!--   BUT the "CHAPTER" and "Waiting for groom..." static parts of it ARE extracted -->
<!-- - Timestamps from new Date().toLocaleTimeString() — not UI copy -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add groom + party strings to messages/en.json</name>
  <read_first>
    - messages/en.json (current content — must be preserved and extended, not overwritten)
    - src/routes/groom/+page.svelte (identify all hardcoded UI strings)
    - src/routes/party/+page.svelte (identify all hardcoded UI strings)
  </read_first>
  <files>messages/en.json</files>
  <action>
Read the current content of messages/en.json first. Then MERGE the following new keys into the existing JSON object (preserve all existing keys from Plan 01 or from the Phase 11 stub — do not delete any key).

Add all keys below to messages/en.json:

GROOM VIEW KEYS:
```json
"groom_role_badge": "You are the Groom",
"groom_role_aria_label": "Role: Groom",
"groom_loading": "Loading...",
"groom_waiting": "Waiting for the game to start...",
"groom_recap_label": "CHAPTER",
"groom_recap_progress": "{current} of {total}"
```

PARTY VIEW KEYS:
```json
"party_waiting_for_groom": "Waiting for groom...",
"party_token_balance_single": "💰 {count} token",
"party_token_balance_plural": "💰 {count} tokens",
"party_earned_counter": "{earned} / 5 earned this challenge",
"party_earn_btn_active": "TAP TO EARN",
"party_earn_btn_capped": "MAX EARNED",
"party_earn_aria_label": "Tap to earn a token",
"party_shop_header": "GROUP SHOP",
"party_shop_empty": "No power-ups available.",
"party_shop_cost_badge": "{cost} tokens",
"party_shop_spend_btn": "Spend",
"party_shop_aria_label": "Group shop",
"party_shop_spend_aria_label": "Spend {cost} tokens on {name}",
"party_balances_header": "TOKEN BALANCES",
"party_balances_aria_label": "Player token balances",
"party_balances_empty": "No group members yet.",
"party_actions_header": "RECENT ACTIONS",
"party_actions_aria_label": "Recent actions",
"party_actions_empty_heading": "No actions yet",
"party_actions_empty_body": "Be the first to use a power-up!",
"party_lobby_waiting": "The game starts when your host is ready.",
"party_spend_reject_toast": "Not enough tokens or spend rejected.",
"party_recap_label": "CHAPTER",
"party_recap_progress": "{current} of {total}",
"party_reward_unlocked_label": "REWARD UNLOCKED"
```

NOTE on token balance display: The party view currently uses `{myBalance === 1 ? "token" : "tokens"}` for singular/plural. Replace with two separate message functions (party_token_balance_single and party_token_balance_plural) called conditionally — this avoids ICU plural syntax which is out of scope for this milestone (I18N-05 is v2). The full expression becomes:
`{myBalance === 1 ? m.party_token_balance_single({ count: myBalance }) : m.party_token_balance_plural({ count: myBalance })}`

NOTE on recap label: Both groom and party views have the same "CHAPTER" recap label — they use separate keys (groom_recap_label and party_recap_label) to allow independent translation if needed, but both map to "CHAPTER" in en.json.

NOTE on groom_recap_progress / party_recap_progress: The recap card shows "{n} of {total}" where both values come from game state. The template "Chapter" prefix in the recap is already captured in groom_recap_label. The progress line is `m.groom_recap_progress({ current: recapChapterIndex + 1, total: $gameState.chapters.length })`.

IMPORTANT: Do not include `action.playerName`, `action.powerUpName`, `powerUp.name`, `powerUp.description`, `player.name`, `chapter.name`, or `announcementData.powerUpName` in the catalog — these are user-authored content from game state (I18N-04).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const j=JSON.parse(f.readFileSync('messages/en.json'));const keys=['groom_role_badge','groom_waiting','groom_recap_label','party_earn_btn_active','party_shop_header','party_balances_header','party_actions_empty_heading','party_lobby_waiting','party_spend_reject_toast','party_reward_unlocked_label'];const missing=keys.filter(k=>!j[k]);if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK — groom+party keys present, total keys:',Object.keys(j).length)"</automated>
  </verify>
  <done>messages/en.json contains all groom_* and party_* keys listed above. Node validation exits 0. Total key count is higher than after Plan 01 (no keys were deleted).</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in groom view</name>
  <read_first>
    - src/routes/groom/+page.svelte (full file — executor must read current state)
    - messages/en.json (verify groom_* key names)
  </read_first>
  <files>src/routes/groom/+page.svelte</files>
  <action>
Add `import * as m from '$lib/paraglide/messages.js';` to the `<script lang="ts">` block imports section (after other imports). Check the file first — if the import is already present, do not add a duplicate.

Then apply the following template replacements:

1. Role badge aria-label:
   `aria-label="Role: Groom"`
   → `aria-label={m.groom_role_aria_label()}`

2. Role badge text:
   `<span class="text-[24px] font-bold" style="color: #0f0f0f;">You are the Groom</span>`
   → `<span class="text-[24px] font-bold" style="color: #0f0f0f;">{m.groom_role_badge()}</span>`

3. Loading placeholder (in `{:else}` branch under player display name):
   `<p class="text-[40px] font-bold text-text-primary">Loading...</p>`
   → `<p class="text-[40px] font-bold text-text-primary">{m.groom_loading()}</p>`

4. Waiting status text:
   `<p class="text-base text-text-secondary text-center">Waiting for the game to start...</p>`
   → `<p class="text-base text-text-secondary text-center">{m.groom_waiting()}</p>`

5. Recap label (in recap-overlay):
   `<p class="recap-label">CHAPTER</p>`
   → `<p class="recap-label">{m.groom_recap_label()}</p>`

6. Recap progress (in recap-content):
   `<p class="recap-progress">{recapChapterIndex + 1} of {$gameState.chapters.length}</p>`
   → `<p class="recap-progress">{m.groom_recap_progress({ current: recapChapterIndex + 1, total: $gameState.chapters.length })}</p>`

LEAVE AS-IS (user-authored content from game state — I18N-04):
- `{myPlayer.name}` — player display name
- `{$gameState.chapters[recapChapterIndex]?.name ?? ""}` — chapter name in recap
- `{recapChapterIndex + 1}` recap number (numeric, not UI copy)
- `{announcementData?.powerUpName ?? ''}` — power-up name
- `{activatingPlayerName}` — player name
- The isSabotage emoji expressions `'⚡'` and `'✨'` — these are decorative emoji, not translatable UI copy

Do NOT change any CSS classes, Svelte reactive logic, imports (other than adding m import), or event handlers.
  </action>
  <verify>
    <automated>grep -n '"You are the Groom"\|"Role: Groom"\|"Waiting for the game\|"Loading\.\.\."\|recap-label">CHAPTER\|" of "' src/routes/groom/+page.svelte</automated>
  </verify>
  <done>grep returns zero matches for the listed hardcoded strings in groom/+page.svelte. All static UI strings replaced with m.*() calls. Player name, chapter name, and power-up name remain as game state expressions.</done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded strings in party/group view</name>
  <read_first>
    - src/routes/party/+page.svelte (full file — executor must read current state before modifying)
    - messages/en.json (verify party_* key names)
  </read_first>
  <files>src/routes/party/+page.svelte</files>
  <action>
Add `import * as m from '$lib/paraglide/messages.js';` to the `<script lang="ts">` block imports. Check first — do not add duplicates.

Apply the following replacements:

ZONE 1 — Groom progress bar:
1. `"Waiting for groom..."` in groom-progress-bar span:
   `{activeChapter?.servedQuestionIndex != null ? "⏱" : "Waiting for groom..."}`
   → `{activeChapter?.servedQuestionIndex != null ? "⏱" : m.party_waiting_for_groom()}`
   (The ⏱ emoji is a decorative game-state indicator — not translatable copy. Leave as-is.)

ZONE 2 — Earn area:
2. Token balance display:
   `💰 {myBalance} {myBalance === 1 ? "token" : "tokens"}`
   → `{myBalance === 1 ? m.party_token_balance_single({ count: myBalance }) : m.party_token_balance_plural({ count: myBalance })}`

3. Earned counter:
   `{earnedThisChallenge} / 5 earned this challenge`
   → `{m.party_earned_counter({ earned: earnedThisChallenge })}`

4. Earn button aria-label:
   `aria-label="Tap to earn a token"`
   → `aria-label={m.party_earn_aria_label()}`

5. Earn button text (inside button element):
   `{earnedThisChallenge >= EARN_CAP ? "MAX EARNED" : "TAP TO EARN"}`
   → `{earnedThisChallenge >= EARN_CAP ? m.party_earn_btn_capped() : m.party_earn_btn_active()}`

ZONE 3 — Shop:
6. Shop header:
   `<p class="text-[14px] text-text-secondary uppercase tracking-[0.15em] mb-3">GROUP SHOP</p>`
   → `<p class="text-[14px] text-text-secondary uppercase tracking-[0.15em] mb-3">{m.party_shop_header()}</p>`

7. Shop empty state:
   `<p class="text-[14px] text-text-secondary text-center py-4">No power-ups available.</p>`
   → `<p class="text-[14px] text-text-secondary text-center py-4">{m.party_shop_empty()}</p>`

8. Shop list aria-label:
   `aria-label="Group shop"`
   → `aria-label={m.party_shop_aria_label()}`

9. Cost badge text:
   `<span class="cost-badge">{powerUp.tokenCost} tokens</span>`
   → `<span class="cost-badge">{m.party_shop_cost_badge({ cost: powerUp.tokenCost })}</span>`

10. Spend button aria-label:
    `aria-label="Spend {powerUp.tokenCost} tokens on {powerUp.name}"`
    → `aria-label={m.party_shop_spend_aria_label({ cost: powerUp.tokenCost, name: powerUp.name })}`
    NOTE: powerUp.name is user-authored content passed as a parameter (I18N-04 compliant).

11. Spend button text:
    `Spend` (inside spend-btn button)
    → `{m.party_shop_spend_btn()}`

SOCIAL WAITING SCREEN:
12. Token balances header:
    `<p class="section-header">TOKEN BALANCES</p>`
    → `<p class="section-header">{m.party_balances_header()}</p>`

13. Balances list aria-label:
    `aria-label="Player token balances"`
    → `aria-label={m.party_balances_aria_label()}`

14. Balances empty state:
    `<li class="px-4 py-3 text-[14px] text-text-secondary">No group members yet.</li>`
    → `<li class="px-4 py-3 text-[14px] text-text-secondary">{m.party_balances_empty()}</li>`

15. Recent actions header:
    `<p class="section-header">RECENT ACTIONS</p>`
    → `<p class="section-header">{m.party_actions_header()}</p>`

16. Actions list aria-label:
    `aria-label="Recent actions"`
    → `aria-label={m.party_actions_aria_label()}`

17. Empty feed heading:
    `<p class="text-[14px] font-bold text-text-primary">No actions yet</p>`
    → `<p class="text-[14px] font-bold text-text-primary">{m.party_actions_empty_heading()}</p>`

18. Empty feed body:
    `<p class="text-[14px] text-text-secondary">Be the first to use a power-up!</p>`
    → `<p class="text-[14px] text-text-secondary">{m.party_actions_empty_body()}</p>`

19. Lobby waiting message:
    `<p class="text-base text-text-secondary text-center mt-4">The game starts when your host is ready.</p>`
    → `<p class="text-base text-text-secondary text-center mt-4">{m.party_lobby_waiting()}</p>`

OVERLAYS:
20. Recap label in party recap-overlay:
    `<p class="recap-label">CHAPTER</p>`
    → `<p class="recap-label">{m.party_recap_label()}</p>`

21. Recap progress:
    `<p class="recap-progress">{recapChapterIndex + 1} of {$gameState.chapters.length}</p>`
    → `<p class="recap-progress">{m.party_recap_progress({ current: recapChapterIndex + 1, total: $gameState.chapters.length })}</p>`

22. Reward overlay label:
    `<p class="reward-label">REWARD UNLOCKED</p>`
    → `<p class="reward-label">{m.party_reward_unlocked_label()}</p>`

TOAST:
23. Spend reject toast text:
    `Not enough tokens or spend rejected.`
    → `{m.party_spend_reject_toast()}`

LEAVE AS-IS (user-authored content from game state — I18N-04):
- `{$gameState.chapters[recapChapterIndex]?.name ?? ""}` — chapter name in recap
- `{revealChapter?.name ?? ""}` — chapter name in reward overlay
- `{revealChapter?.reward ?? ""}` — reward text in reward overlay
- `{action.playerName} used {action.powerUpName}` — dynamic action feed text (these are player/power-up names from game state, not static UI copy)
- `{powerUp.name}`, `{powerUp.description}` — power-up name and description (user-authored game content)
- `{activatingPlayerName}` — player name in announcement overlay
- `{announcementData?.powerUpName ?? ''}` — power-up name in announcement overlay
- `{chapterLabel}` derived — already built from game state; the "CHAPTER" label in groom-progress-bar is from chapterLabel so leave that span as-is
  </action>
  <verify>
    <automated>grep -n '"Waiting for groom\|" token"\|" tokens"\|"/ 5 earned\|"TAP TO EARN\|"MAX EARNED\|"Tap to earn\|"GROUP SHOP\|"No power-ups\|"Group shop\|" tokens"\|"Spend"\|"TOKEN BALANCES\|"Player token\|"No group members\|"RECENT ACTIONS\|"Recent actions\|"No actions yet\|"Be the first\|"The game starts\|"Not enough tokens\|"REWARD UNLOCKED"' src/routes/party/+page.svelte</automated>
  </verify>
  <done>grep returns zero matches for the listed hardcoded strings in party/+page.svelte. All static UI copy replaced with m.*() calls. Action feed strings, power-up names, chapter names, and reward text remain as game state expressions.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the build:

```bash
cd /Users/espetec/dev/octapp && npm run build 2>&1 | tail -20
```

A clean build (exit 0) confirms Paraglide generated typed message functions for all new groom_* and party_* keys and that both route files compile without TypeScript errors.

Also confirm user-authored content is absent from the catalog:
```bash
node -e "const j=JSON.parse(require('fs').readFileSync('messages/en.json'));const userContent=['player','chapter.name','powerUp','reward','clue','trivia'];const keys=Object.keys(j);console.log('Keys containing game-state terms:',keys.filter(k=>userContent.some(t=>j[k].toLowerCase().includes(t))))"
```
This should return an empty array (no catalog values contain actual user-authored runtime content).
</verification>

<success_criteria>
1. messages/en.json contains all groom_* and party_* keys (≥25 new keys added to existing catalog).
2. grep for hardcoded UI strings in groom/+page.svelte and party/+page.svelte returns zero matches for the strings listed in the verify blocks.
3. `npm run build` exits 0.
4. User-authored content (player.name, chapter.name, powerUp.name, powerUp.description, reward text, action strings) is not present in messages/en.json and renders directly from game state.
</success_criteria>

<output>
After completion, create `.planning/phases/12-string-catalog/12-02-SUMMARY.md`
</output>
