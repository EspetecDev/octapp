---
phase: 06-three-device-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [VALID-01]
must_haves:
  truths:
    - "Admin is logged into the live dashboard and the session code is visible"
    - "At least one chapter is configured with minigame type 'sensor' and saved"
    - "The 'Unlock Chapter' button is present on the admin dashboard (canUnlock is true)"
  artifacts:
    - path: "https://octapp-production.up.railway.app/admin"
      provides: "Live admin dashboard showing session code and chapter control"
  key_links:
    - from: "Admin setup form (/admin/setup)"
      to: "Admin dashboard chapter count"
      via: "SAVE_SETUP message → server state → canUnlock derived"
      pattern: "chapters.length > 0"
---

<objective>
Set up the admin session and configure the minimum viable game content so all three devices can join and the chapter unlock test can proceed.

Purpose: Every subsequent validation step depends on a configured session — without chapters saved and the admin dashboard ready, VALID-01 (join sync) and VALID-02 (chapter unlock) cannot be tested. This wave must complete before any device joins.

Output: Admin is on the live dashboard, one sensor chapter is saved, the session code is visible on screen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-three-device-validation/06-CONTEXT.md
@.planning/phases/06-three-device-validation/06-RESEARCH.md

Key URLs:
- Admin bookmark: https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65
- Setup URL: https://octapp-production.up.railway.app/admin/setup?token=ebf5c8b171e554b037df2d4225b3bc65
- Live URL: https://octapp-production.up.railway.app

Research pitfalls that apply here:
- Pitfall 1: Admin must complete setup BEFORE any device joins — UNLOCK_CHAPTER fails silently if chapters.length === 0
- Pitfall 2: Never navigate directly to /groom or /party — always join through the root URL with the session code
- Pitfall 6: Do NOT press Android back button during any test step
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Open admin dashboard and navigate to game setup</name>
  <read_first>
    - Admin bookmark URL: https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65
    - Setup URL: https://octapp-production.up.railway.app/admin/setup?token=ebf5c8b171e554b037df2d4225b3bc65
    - RESEARCH.md "Pitfall 1" — chapters.length must be > 0 before unlock; setup must happen before any player joins
    - RESEARCH.md "Admin Setup Guide" — one sensor chapter is the minimum viable configuration
  </read_first>
  <action>
    On ADMIN (PC/laptop browser):

    1. Open the admin bookmark in a new browser tab:
       https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65

    2. Confirm the admin dashboard loads — you should see a 6-character session code displayed on screen (e.g., "ABC123"). If you see a 401 error or a blank page, stop and verify the URL includes the correct token.

    3. Click the "Configure Game" link (visible while in lobby state). This navigates to:
       https://octapp-production.up.railway.app/admin/setup?token=ebf5c8b171e554b037df2d4225b3bc65

    4. On the setup form, fill in the following fields for Chapter 1:
       - Chapter name: "Test Chapter"
       - Minigame type: select "Sensor" (tilt / balance challenge)
       - Scavenger clue: "Find something red nearby"
       - Scavenger hint: (leave blank)
       - Reward: "You win!"
       - Power-up catalog: leave empty
       - Starting tokens: 0

    5. Tap or click "Save Setup".

    6. Confirm a success message appears (or the page updates to show the saved configuration). If an error appears, check that all required fields have values.
  </action>
  <acceptance_criteria>
    - The admin dashboard is visible at https://octapp-production.up.railway.app/admin — a 6-character session code is displayed on screen
    - The setup form submits without an error message
    - After saving, the setup form reflects the saved chapter ("Test Chapter" and minigame type "Sensor" are visible)
    - No browser console errors appear (open DevTools → Console to verify)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Return to admin dashboard and confirm chapter control is ready</name>
  <read_first>
    - Admin bookmark URL: https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65
    - RESEARCH.md "Pitfall 1" — canUnlock is false when chapterCount === 0; after setup the button must now be present and enabled
    - RESEARCH.md "How VALID-02 Works" — admin must NOT tap "Unlock Chapter" yet; all devices must join first (Pitfall 5)
  </read_first>
  <action>
    On ADMIN (PC/laptop browser):

    1. Navigate back to the admin dashboard:
       https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65

    2. Confirm the dashboard shows:
       - Session code (6 characters) — write this code down; you will enter it on both phones
       - Chapter control section showing "Test Chapter" or chapter count = 1
       - "Unlock Chapter" (or "Start Game") button — it must be VISIBLE and ENABLED (not greyed out)
       - Player list section (currently empty — no players have joined yet)

    3. DO NOT tap "Unlock Chapter" yet. All three devices must join before unlock (see Pitfall 5 in RESEARCH.md — late joiners miss the recap card).

    4. Note the session code on a piece of paper or a second screen. You will need it in Plan 02.
  </action>
  <acceptance_criteria>
    - The admin dashboard displays a 6-character session code
    - The "Unlock Chapter" or "Start Game" button is visible and appears enabled (not greyed out / not showing "No chapters configured")
    - The player list shows 0 players connected
    - No console errors in DevTools
  </acceptance_criteria>
</task>

</tasks>

<verification>
Wave 1 is complete when:
- Admin dashboard is open on PC with a valid session code
- At least 1 sensor chapter is saved in setup
- "Unlock Chapter" button is present and enabled
- No devices have joined yet (clean state for VALID-01)
</verification>

<success_criteria>
Admin is on the live dashboard with a session code. One sensor chapter is configured and saved. The "Unlock Chapter" button is enabled. The tester has noted the session code for use in the next plan. No players have joined yet.
</success_criteria>

<output>
After completion, create `.planning/phases/06-three-device-validation/06-01-SUMMARY.md` with:
- Session code used (or note if it changes between runs)
- Setup saved: yes/no
- Unlock button present: yes/no
- Any console errors observed
</output>
