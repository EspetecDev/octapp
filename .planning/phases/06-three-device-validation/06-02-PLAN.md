---
phase: 06-three-device-validation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [VALID-01, VALID-02]
must_haves:
  truths:
    - "All three role views are correct: admin dashboard visible on PC, groom waiting screen on Android phone, party waiting screen on second device"
    - "Admin player list shows both the groom name and the party member name"
    - "After admin taps Unlock Chapter, the recap card appears on groom screen within 2 seconds"
    - "After admin taps Unlock Chapter, the recap card appears on party screen within 2 seconds"
    - "Admin dashboard reflects the chapter has advanced (activeChapterIndex changes)"
  artifacts:
    - path: "https://octapp-production.up.railway.app"
      provides: "Join page where groom and party enter session code"
    - path: "https://octapp-production.up.railway.app/groom"
      provides: "Groom waiting screen after join"
    - path: "https://octapp-production.up.railway.app/party"
      provides: "Party waiting screen after join"
  key_links:
    - from: "Join form (root URL)"
      to: "/groom or /party"
      via: "STATE_SYNC gameState subscription — auto-redirect when name+role match"
      pattern: "goto('/groom') or goto('/party')"
    - from: "Admin UNLOCK_CHAPTER tap"
      to: "Groom and Party recap card"
      via: "STATE_SYNC broadcast → $effect watches activeChapterIndex → showRecap = true"
      pattern: "activeChapterIndex null → 0"
---

<objective>
Validate VALID-01 (all three devices join simultaneously and see correct role views) and VALID-02 (chapter unlock broadcasts to all devices within 2 seconds).

Purpose: These are the two blocking acceptance criteria for Phase 6. VALID-01 confirms the join flow and real-time player list work on real hardware. VALID-02 confirms the STATE_SYNC broadcast reaches all connected clients fast enough to be usable during a live party.

Output: Pass/fail result for VALID-01 and VALID-02 recorded in SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-three-device-validation/06-CONTEXT.md
@.planning/phases/06-three-device-validation/06-RESEARCH.md

Key URLs:
- Live URL (join page): https://octapp-production.up.railway.app
- Admin dashboard: https://octapp-production.up.railway.app/admin?token=ebf5c8b171e554b037df2d4225b3bc65

Critical ordering requirement (from RESEARCH.md Pitfall 5):
- ALL devices must join BEFORE admin taps "Unlock Chapter"
- Late joiners after unlock miss the recap card — this is by design, not a bug
- If a device joins after unlock, the initialSyncDone guard suppresses the recap card — test result is invalid

Device assignment (per D-06):
- ADMIN: PC/laptop browser tab (already open from Plan 01)
- GROOM: Android phone (primary)
- PARTY: Second Android phone, tablet, or incognito/different browser tab on PC

Pitfall 3 warning: If PARTY uses the same browser as GROOM on the same machine, localStorage collision occurs — party tab reads groom's playerId and auto-redirects to /groom. Use a DIFFERENT BROWSER or incognito window.
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: VALID-01 — All three devices join the session simultaneously</name>
  <read_first>
    - RESEARCH.md "How VALID-01 Works" — join flow: code → name → role → STATE_SYNC → auto-redirect
    - RESEARCH.md "Pitfall 2" — always join via root URL /; never navigate directly to /groom or /party
    - RESEARCH.md "Pitfall 3" — if PARTY uses same browser as GROOM on same PC, localStorage collision causes wrong redirect; use incognito or different browser
    - RESEARCH.md "Pitfall 5" — do NOT unlock chapter until all 3 devices have joined and are on their role screens
    - RESEARCH.md "Pitfall 7" — label every action with the device name to avoid acting on the wrong screen
    - Session code from Plan 01 Task 2 (you noted it down)
  </read_first>
  <action>
    Prerequisite: Plan 01 is complete. Admin dashboard is open on PC. Session code is noted.

    Step 1 — On GROOM (Android phone, Chrome):
    1. Open Chrome on the Android phone
    2. Navigate to: https://octapp-production.up.railway.app
    3. Enter the session code (the 6-character code from the admin dashboard)
    4. Enter a name for the groom (e.g., "Groom")
    5. Select "I'm the Groom"
    6. Tap Join
    7. Wait for auto-redirect — the page should route to /groom and show a waiting screen

    Step 2 — On PARTY (second Android phone, OR incognito/different browser on PC):
    - If using a second phone: open its browser, navigate to https://octapp-production.up.railway.app
    - If using a second browser tab on PC: open an INCOGNITO window in Chrome, or use Firefox — do NOT use the same browser as GROOM (localStorage collision, Pitfall 3)
    1. Navigate to: https://octapp-production.up.railway.app
    2. Enter the same session code
    3. Enter a name for the party member (e.g., "Party1")
    4. Select "I'm in the Group"
    5. Tap/click Join
    6. Wait for auto-redirect — the page should route to /party and show the party waiting screen

    Step 3 — On ADMIN (PC):
    1. Watch the admin dashboard player list
    2. Confirm both "Groom" and "Party1" names appear in the list
    3. Confirm the dashboard still shows lobby state (chapter not yet unlocked)

    Step 4 — Cross-check all three screens:
    - ADMIN: shows session code + player list with 2 players
    - GROOM: shows the groom waiting screen at /groom
    - PARTY: shows the party waiting screen at /party
  </action>
  <acceptance_criteria>
    - GROOM device: browser has routed to /groom; the groom waiting screen is visible (not the join form, not a blank page)
    - PARTY device: browser has routed to /party; the party waiting screen is visible (not the join form, not a blank page)
    - ADMIN dashboard: player list shows both player names ("Groom" and "Party1") — confirming STATE_SYNC was received by the admin client
    - No console errors on any device (check with DevTools on PC; on Android, use Chrome remote debugging or note any on-screen error messages)
    - All three screens reflect the same lobby state (no chapter unlocked yet)
    Result to record: VALID-01 PASS or VALID-01 FAIL (with observed failure description)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: VALID-02 — Admin unlocks chapter; all devices show recap card within 2 seconds</name>
  <read_first>
    - RESEARCH.md "How VALID-02 Works" — UNLOCK_CHAPTER → STATE_SYNC broadcast → $effect on activeChapterIndex → showRecap = true (3s auto-dismiss)
    - RESEARCH.md "Pitfall 5" — all devices must already be joined before this step; do NOT re-join after unlock
    - ROADMAP.md §Phase 6 Success Criteria #2 — "Admin unlocks a chapter and all three devices display the chapter transition card within 2 seconds — verified by watching screens side by side"
    - D-01 (CONTEXT.md): if recap card appears on groom but not party (or vice versa), log the failure and continue — do NOT stop the session
  </read_first>
  <action>
    Prerequisite: Task 1 is complete. All three devices are on their role screens. DO NOT let any device navigate away.

    Step 1 — Position all three screens so you can see them simultaneously:
    - PC monitor: admin dashboard
    - Android phone 1 (GROOM): groom waiting screen
    - Second device (PARTY): party waiting screen
    Lay phones next to the keyboard if solo testing.

    Step 2 — On ADMIN (PC):
    1. Locate the "Unlock Chapter" button (or "Start Game" / "Unlock Chapter 1") on the admin dashboard
    2. Take a breath — you need to watch all screens immediately after clicking
    3. Click "Unlock Chapter"
    4. Immediately shift eyes to both phone screens

    Step 3 — Observe all three screens within 2 seconds of clicking:
    - GROOM (Android): a chapter recap/transition card should slide in or appear — it shows the chapter name ("Test Chapter") and auto-dismisses after ~3 seconds
    - PARTY (second device): same recap card should appear
    - ADMIN (PC): the chapter control section should update to reflect activeChapterIndex = 0 (chapter is now active)

    Step 4 — After the recap card auto-dismisses:
    - GROOM should transition to the sensor minigame screen (showing the tilt meter or a "tap to enable" button)
    - PARTY should transition to the group participation screen
    - ADMIN should show the chapter is active

    Step 5 — Record the result:
    - If all three screens showed the recap card within ~2 seconds: VALID-02 PASS
    - If one or more screens did NOT show the recap card: VALID-02 FAIL — note which device(s) missed it and any observed delay
  </action>
  <acceptance_criteria>
    - Within 2 seconds of admin clicking "Unlock Chapter": the recap card (showing "Test Chapter" or similar chapter transition overlay) is visible on GROOM screen
    - Within 2 seconds of admin clicking "Unlock Chapter": the recap card is visible on PARTY screen
    - Admin dashboard reflects chapter has advanced (chapter control shows active state, not lobby)
    - The recap card auto-dismisses after ~3 seconds without manual action
    - After dismissal, GROOM screen shows the sensor minigame interface (tilt meter or "enable sensor" prompt)
    Result to record: VALID-02 PASS or VALID-02 FAIL (note which screen(s) failed and any observed delay in seconds)
  </acceptance_criteria>
</task>

</tasks>

<verification>
VALID-01 and VALID-02 results are the gate for Phase 6 closure (per D-03):
- Both PASS → Phase 6 is complete; proceed to Plan 03 for non-blocking tests
- VALID-01 FAIL → log failure details in SUMMARY.md Phase 7 work items; determine if VALID-02 can still be attempted
- VALID-02 FAIL → log failure details; Phase 6 remains open; failure goes to Phase 7 for investigation
</verification>

<success_criteria>
VALID-01: All three devices (admin PC, groom Android, party device) are simultaneously on their correct role screens with the admin player list confirming both groom and party are connected.

VALID-02: Admin clicks "Unlock Chapter" and the recap card appears on both groom and party screens within 2 seconds, confirmed by the solo tester watching all screens simultaneously.

Per D-03: Phase 6 closes when both VALID-01 and VALID-02 pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-three-device-validation/06-02-SUMMARY.md` with the pass/fail checklist:

```
## VALID-01: Simultaneous Join Sync
- [ ] GROOM redirected to /groom after join
- [ ] PARTY redirected to /party after join
- [ ] ADMIN player list shows both players
- [ ] No console errors on any device
Result: PASS / FAIL

## VALID-02: Chapter Unlock Broadcast
- [ ] Recap card appeared on GROOM within 2 seconds
- [ ] Recap card appeared on PARTY within 2 seconds
- [ ] Admin dashboard reflected chapter advance
- [ ] Recap card auto-dismissed after ~3 seconds
Result: PASS / FAIL

## Phase 7 Work Items (if any)
(list any failures observed with format from RESEARCH.md)
```
</output>
